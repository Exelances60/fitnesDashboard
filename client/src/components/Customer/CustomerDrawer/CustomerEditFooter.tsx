import { Button } from "antd";
import React from "react";

const CustomerEditFooter = () => {
  return (
    <Button type="primary" form="editCustomerForm" htmlType="submit">
      Save
    </Button>
  );
};

export default CustomerEditFooter;
